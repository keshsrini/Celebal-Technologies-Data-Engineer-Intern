# Enter your code here. Read input from STDIN. Print output to STDOUT
import re

T = int(input())
for _ in range(T):
    pattern = input().strip()
    try:
        re.compile(pattern)
        print("True")
    except:
        print("False")
